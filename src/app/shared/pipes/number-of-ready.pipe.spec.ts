import { NumberOfReadyPipe } from './number-of-ready.pipe';

describe('NumberOfReadyPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfReadyPipe();
    expect(pipe).toBeTruthy();
  });
});
